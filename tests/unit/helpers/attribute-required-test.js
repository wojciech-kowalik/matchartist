import { attributeRequired } from 'frontend/helpers/attribute-required';
import { module, test } from 'qunit';

module('Unit | Helper | attribute required');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = attributeRequired([42]);
  assert.ok(result);
});
