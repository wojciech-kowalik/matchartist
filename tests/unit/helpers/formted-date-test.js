import { formtedDate } from 'frontend/helpers/formted-date';
import { module, test } from 'qunit';

module('Unit | Helper | formted date');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formtedDate([42]);
  assert.ok(result);
});
