import { activeTab } from 'frontend/helpers/active-tab';
import { module, test } from 'qunit';

module('Unit | Helper | active tab');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = activeTab([42]);
  assert.ok(result);
});
