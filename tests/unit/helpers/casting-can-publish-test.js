import { castingCanPublish } from 'frontend/helpers/casting-can-publish';
import { module, test } from 'qunit';

module('Unit | Helper | casting can publish');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = castingCanPublish([42]);
  assert.ok(result);
});
